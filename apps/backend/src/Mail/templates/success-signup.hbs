<!-- success-signup.hbs -->
<!doctype html>
<html lang="zh-Hant">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta http-equiv="x-ua-compatible" content="ie=edge" />
  <title>{{appName}} 註冊成功通知</title>
  <!-- 部分客戶端會擷取第一行做為預覽文字 -->
  <meta name="x-preheader" content="{{appName}} 註冊成功，請妥善保管您的帳密。">
  <style>
    /* 基本 Reset（盡量簡短，避免被客戶端刪除） */
    body, table, td, p, a { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang TC", "Noto Sans TC", "Microsoft JhengHei", Roboto, Helvetica, Arial, sans-serif; }
    img { border: 0; outline: none; text-decoration: none; }
    table { border-collapse: collapse; }
    a { color: #0ea5e9; text-decoration: none; }
    /* 亮色主題 */
    :root {
      --bg: #f6f8fb;
      --card: #ffffff;
      --text: #0f172a;       /* slate-900 */
      --muted: #64748b;      /* slate-500 */
      --primary: #0ea5e9;    /* sky-500 */
      --primary-dark: #0284c7; /* sky-600 */
      --border: #e2e8f0;     /* slate-200 */
      --code-bg: #0f172a0d;  /* 淡淡底 */
    }
    /* 暗色主題（支援的客戶端會套用） */
    @media (prefers-color-scheme: dark) {
      :root {
        --bg: #0b1220;
        --card: #111827;   /* gray-900 */
        --text: #e5e7eb;   /* gray-200 */
        --muted: #94a3b8;  /* slate-400 */
        --primary: #7dd3fc; /* sky-300 */
        --primary-dark: #38bdf8; /* sky-400 */
        --border: #1f2937;  /* gray-800 */
        --code-bg: #111827; /* gray-900 */
      }
    }
    /* 主要樣式 */
    body {
      margin: 0; padding: 0; background: var(--bg); color: var(--text);
    }
    .container {
      width: 100%; background: var(--bg); padding: 32px 0;
    }
    .wrapper {
      width: 100%; max-width: 640px; margin: 0 auto; background: var(--card);
      border: 1px solid var(--border); border-radius: 14px; overflow: hidden;
      box-shadow: 0 6px 24px rgba(2,6,23,0.06);
    }
    .header {
      padding: 28px 32px; border-bottom: 1px solid var(--border);
      text-align: center; background: linear-gradient(180deg, rgba(14,165,233,0.06), transparent);
    }
    .brand {
      font-weight: 700; font-size: 18px; letter-spacing: .4px;
      color: var(--primary);
    }
    .title {
      margin: 10px 0 0; font-size: 22px; font-weight: 700;
    }
    .content {
      padding: 24px 32px 8px; line-height: 1.75; font-size: 15px; color: var(--text);
    }
    .muted {
      color: var(--muted);
    }
    .kv {
      margin: 16px 0 0;
      padding: 14px 16px; background: var(--code-bg);
      border: 1px dashed var(--border); border-radius: 12px; font-family: ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,"Liberation Mono","Courier New",monospace;
    }
    .kv dl { margin: 0; display: grid; grid-template-columns: 96px 1fr; row-gap: 8px; column-gap: 12px; }
    .kv dt { color: var(--muted); }
    .kv dd { margin: 0; font-weight: 600; }
    .btn-row {
      padding: 8px 32px 24px;
    }
    .btn {
      display: inline-block;
      background: var(--primary); color: #ffffff !important;
      padding: 12px 18px; border-radius: 10px; font-weight: 700; font-size: 14px;
    }
    .btn:hover { background: var(--primary-dark); }
    .divider {
      height: 1px; background: var(--border); margin: 16px 32px;
    }
    .footer {
      padding: 0 32px 28px; font-size: 12px; color: var(--muted);
    }
    .note {
      margin-top: 8px;
      padding: 10px 12px; border-left: 3px solid var(--primary);
      background: linear-gradient(90deg, rgba(14,165,233,0.08), transparent);
      border-radius: 8px;
    }
    @media (max-width: 480px) {
      .header { padding: 22px 20px; }
      .content { padding: 20px; }
      .btn-row { padding: 8px 20px 20px; }
      .divider { margin: 14px 20px; }
      .footer { padding: 0 20px 22px; }
      .kv dl { grid-template-columns: 88px 1fr; }
    }
  </style>
</head>
<body>
  <!--[if mso]>
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0"><tr><td align="center">
  <![endif]-->
  <div class="container">
    <div class="wrapper">
      <!-- Header -->
      <div class="header">
        <div class="brand">{{appName}}</div>
        <div class="title">註冊成功通知</div>
      </div>

      <!-- Content -->
      <div class="content">
        <p><strong>{{username}}</strong> 您好，</p>
        <p>
          您已成功註冊 <strong>{{appName}}</strong>。以下是您的登入資訊，請<strong>妥善保管</strong>：
        </p>

        <div class="kv">
          <dl>
            {{#if email}}
              <dt>電子信箱</dt><dd>{{email}}</dd>
            {{/if}}
            <dt>登入帳號</dt><dd>{{username}}</dd>
            <dt>登入密碼</dt><dd>{{password}}</dd>
          </dl>
        </div>

        <p class="muted" style="margin-top:12px;">
          小提醒：為了您的帳號安全，建議您首次登入後立即更換密碼，並定期更新。
        </p>

        {{#if note}}
          <div class="note">
            {{note}}
          </div>
        {{/if}}
      </div>

      <!-- Primary Action -->
      <div class="btn-row">
        <a class="btn" href="{{loginUrl}}" target="_blank" rel="noopener">立即登入系統</a>
      </div>

      <div class="divider"></div>

      <!-- Secondary / Help -->
      <div class="content" style="padding-top:0;">
        <p style="margin:0 0 8px;">
          若您之後忘記密碼，請至系統使用「忘記密碼」功能重新設定。
          {{#if resetUrl}}
            您也可以直接前往：<a href="{{resetUrl}}" target="_blank" rel="noopener">{{resetUrl}}</a>
          {{/if}}
        </p>
        {{#if supportEmail}}
          <p class="muted" style="margin:0;">
            若有任何問題，歡迎聯絡我們：<a href="mailto:{{supportEmail}}">{{supportEmail}}</a>
          </p>
        {{/if}}
      </div>

      <!-- Footer -->
      <div class="footer">
        本郵件由系統自動發送，請勿直接回覆。若您未曾註冊 {{appName}}，請忽略此郵件。
        <br />
        &copy; {{appName}} – All rights reserved.
      </div>
    </div>
  </div>
  <!--[if mso]></td></tr></table><![endif]-->
</body>
</html>
